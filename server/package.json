{
  "name": "lan-party-game-server",
  "version": "1.0.0",
  "description": "Minimal Express + WebSocket relay server for LAN party game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "vitest --run"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "fast-check": "^4.8.0",
    "vitest": "^1.6.0"
  }
}
